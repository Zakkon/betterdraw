If LayerSettings are completely corrupted or missing:

Print error message if presumed that settings existed before but were lost
Create new layersettings, with default settings
Delete all strokes
Don't delete saved image, user might want to recover it
Save over the image later
-----------------------------------
If only one important value of LayerSettings is corrupted:

Print error message
Create new layersettings, with default settings
Delete all strokes
Don't delete saved image, user might want to recover it
Save over the image later
-----------------------------------
If strokes are corrupted or missing:
